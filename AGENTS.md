# Agent Notes

When wiring another Android repository's CI to this server, use [docs/pushing-to-f-server.md](docs/pushing-to-f-server.md). The CI job needs only an `FSERVER_API_KEY` secret from the human/admin and the APK artifact path.
